#!/bin/sh
# Remove Cursor agent attribution from commit messages (local hook).
file="$1"
[ -f "$file" ] || exit 0
tmp="${file}.cursor-strip"
grep -v -E -i '(co-authored-by:.*cursor|made-with:.*cursor)' "$file" > "$tmp" 2>/dev/null || cp "$file" "$tmp"
mv "$tmp" "$file"
exit 0
