services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: fratton2026
      MONGO_INITDB_DATABASE: pulse
    volumes:
      - mongo_data:/data/db

  mongo-express:
    image: mongo-express:latest
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: fratton2026
      ME_CONFIG_MONGODB_URL: mongodb://root:fratton2026@mongo:27017/
      ME_CONFIG_BASICAUTH: "false"
    depends_on:
      - mongo

  backend:
    build: ./pulse-backend
    ports:
      - "8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-Dspring.data.mongodb.uri=mongodb://root:fratton2026@mongo:27017/pulse?authSource=admin"
      SPRING_DATA_MONGODB_URI: mongodb://root:fratton2026@mongo:27017/pulse?authSource=admin
      JWT_SECRET: ${JWT_SECRET:-local-dev-jwt-secret-min-32-characters!!}
    depends_on:
      - mongo

volumes:
  mongo_data:
